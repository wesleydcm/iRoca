import { screen, render } from "@testing-library/react";
import "@testing-library/jest-dom/extend-expect";
/* import userEvent from "@testing-library/user-event"; */
import MenuDesktop from "../desktop/index";

/* describe("when everything is ok", () => {
  test("Should clicking on the cart link is expected to be class selected", () => {
    render(<MenuDesktop />);
    const goToLink = screen.getByTestId("linkToCart");
     userEvent.click(goToLink);

    expect(goToLink).toHaveClass("selected");
  });
});
 */
