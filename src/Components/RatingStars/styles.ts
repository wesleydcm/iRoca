import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  flex-wrap: nowrap;
  align-items: center;
  p {
    font-size: 15px;
    color: var(--gray600);
    font-family: var(--poppins);
    margin: 0 2px;
  }
`;
